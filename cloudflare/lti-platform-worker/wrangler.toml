name = "modernlms-lti-platform"
main = "src/index.ts"
compatibility_date = "2025-03-01"

[vars]
LTI_NONCE_TTL_SECONDS = "300"
LTI_STATE_TTL_SECONDS = "300"
# URL of the ADTA registration UI page (where admin is sent to complete dynamic reg)
LTI_TOOL_REGISTRATION_URL = "https://staging-app.alldayta.com/lti-register"
